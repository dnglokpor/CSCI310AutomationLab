const filter = require("../../src/filterByTerm");

// Check Filter Function orking properly
describe("Filter function", () => {
    test("Search by term fucntionality", () => {
        const input = [

        ];
    });
});